events {}

http {
    server {
        listen 80;

        location / {
            root /usr/share/nginx/html;

            add_header Cache-Control "no-cache, no-store";
            autoindex on;
        }
    }
}
